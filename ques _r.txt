r-1)create a php responsive form where u take name phone number and address from user and store the details in associative array .keeep new user option.create a function for the user to sign up where u ask the user to insert password and confirm password (min 10 char ,2 caps,one number,one(_,@,$)
2)create a html form where the user will name ,email id , phone number,address, purchase history(premium,gold,standard).  if the details of the user is allready is there then send it as asynchronous response to the html file and display the details as session(phone no (10 digit starting with 6_0_9,name should b alphabetic,email in strict email format with 6 dot domains (like .mil, .org , .gov, .com, .edu, .in))
